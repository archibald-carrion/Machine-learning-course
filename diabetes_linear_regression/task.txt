Regresión lineal
Regresar a: Regresión
Descargue el archivo diabetes.csv.

Cree un cuaderno Python.

Haga un reseña histórica sobre la regresión lineal y discutir brevemente las aplicaciones de la regresión lineal. Usualmente, se usa una regresión para predecir los valores ausentes de una variable basándose en su relación con otras variables del conjunto de datos. Hay regresión lineal, no lineal, logística, logarítmica, univariada, multivariada, entre otras.

Explore los datos de diabetes.csv.  Para ello despliegue un correlograma.  Puede ser así:

import seaborn as sns
import matplotlib.pyplot as plt

# Computa la matriz de correlación
corr = pima.corr() # pima es un dataframe
sns.heatmap(corr, annot = True, vmin=-1, vmax=1, center= 0, cmap= 'coolwarm')

# Densidad
sns.pairplot(pima, diag_kind="kde")

Indique cuales son las 3 variables con mayor correlación con insulina.

Cree una regresión lineal en la que la glucosa sea la variable independiente y la insulina la variable dependiente.

Grafique la regresión lineal y los individuos.